# Cloudflare D1 database id. The local development database is SQLite.
CLOUDFLARE_D1_DATABASE_ID=

# Optional local SQLite path; defaults to .data/minesweeper.sqlite.
LOCAL_DB_PATH=

# Use a long random secret in production.
# Generate one with:
#   openssl rand -base64 32
JWT_SECRET=
