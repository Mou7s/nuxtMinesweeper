# 扫雷 (Minesweeper) 开发计划 (TODO)

## 最终目标
1. 🌌 **无限延伸的地图**: 打破传统固定边界，探索无尽的雷区。
2. ⚔️ **多人对战/协作**: 支持多名玩家在同一个无限地图上共同扫雷或进行对抗。

---

## 阶段一：单机基础版 MVP (当前建议聚焦)
*💡 建议：万丈高楼平地起，不要一开始就考虑“无限”和“多人”，先把最核心的扫雷逻辑写健壮。*

- [x] **初始化项目状态**
  - [x] 确认 UI 组件库和样式方案 (TailwindCSS 已配置)，确保开发体验良好。
  - [x] 引入 `Pinia` 或使用 `useState` 来统一管理全局游戏状态，解耦 UI 和逻辑。
- [x] **核心扫雷逻辑设计 (固定大小)**
  - [x] 定义数据结构 (如格子坐标、是否是雷、周围雷数、翻开状态、插旗状态)。
  - [x] 实现生成固定大小网格 (例如 10x10, 10个雷)。
  - [x] 随机布雷算法 (可以考虑点击第一下之后再布雷，保证第一下绝对不踩雷)。
  - [x] 预计算并填充非雷格子的“周围地雷数量”。
- [x] **用户交互**
  - [x] 左键点击：翻开格子。
  - [x] 右键点击 (或移动端长按)：插旗标记地雷 / 取消标记。
- [x] **核心算法与胜负判定**
  - [x] **泛洪展开算法 (Flood Fill)**：当点击到空白格(周围无雷为0)时，自动向外展开周围相邻的格子。
  - [x] 游戏失败：左键点到地雷，展示所有地雷。
  - [x] 游戏胜利：所有非雷格子都被翻开。

---

## 阶段二：渲染重构 (为无限地图铺垫)
*💡 建议：核心逻辑跑通后，我们需要将“数据”和“渲染”分开，不能把成千上万的格子全用 DOM 渲染。*

- [x] **视口渲染 (Viewport) 与拖拽**
  - [x] 使用 `Canvas` 或 `DOM 虚拟滚动` 技术，只渲染屏幕内可见范围的格子。
  - [x] 支持鼠标中键/右键拖拽或触摸拖拽，来实现整个地图的平移视角。
- [x] **程序化生成与区块化 (Chunking)**
  - [x] 引入“区块”(Chunk) 的概念 (例如 16x16 的格子作为一个 Chunk)。
  - [x] 根据固定**随机种子 (Seed)** 和噪声算法实时生成坐标所在 Chunk 的地雷分布。这样无需存储无限大的地图，只需按坐标演算。

---

## 阶段三：网络同步 (多人游戏)
*💡 建议：前端单机无限地图完善后，将逻辑迁移至服务端。*

- [ ] **搭建 WebSocket 服务**
  - [ ] 在 Nuxt 3 服务端引擎 (Nitro) 或外部 Node.js 服务中集成 WebSocket。
- [ ] **状态同步与服务端验证**
  - [ ] 后端掌握“地图生成”和“逻辑判定”。
  - [ ] 前端不再自己判断胜负和计算翻开，只负责发送操作指令 (`Click(x, y)` / `Flag(x, y)`)。
  - [ ] 后端验证操作后，广播更新的 Chunk 数据和状态给所有连接的客户端。
- [ ] **多人协作体验**
  - [ ] 在地图上展示其他玩家的鼠标位置/光标。
  - [ ] 积分机制 (挖出数字得分、排雷得分、踩雷扣分等)。
