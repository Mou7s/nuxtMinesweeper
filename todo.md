# Infinite Minesweeper 性能表现 TODO

检查日期：2026-05-17

## 当前性能判断

项目现在已经能在线玩，性能瓶颈也比较清楚：不是 Nuxt 本身慢，而是多人无限棋盘会天然放大几个成本。

当前最值得优化的路径：

- Canvas 棋盘每次更新都会重绘整个可视区域。
- 世界快照会把所有已翻开/已插旗格子一次性发给新连接。
- `save()` 持久化时会扫描整个 `blocks` Map。
- 大面积零展开会一次性产生大量格子更新和计分更新。
- 鼠标移动 cursor 每 50ms 发一次，玩家多时广播会变重。
- 客户端每个格子都用 Vue `reactive` 包一层，数据多时会增加内存和更新成本。

目标不是盲目改架构，而是先把“看得到、摸得到”的体验做好：打开更快、拖动更稳、点击反馈更快、多人时不抖。

## P0：先建立性能基准

### 1. 加一个本地性能调试面板

原因：
现在感觉“延迟高”或“卡”，但缺少数字。没有数字会很难判断优化是否真的有效。

要做：

- 在开发环境显示一个小型性能面板。
- 展示：
  - 当前 FPS；
  - 当前可视格子数；
  - 本地缓存格子数；
  - 最近一次 WebSocket 消息大小；
  - 最近一次 `update` 的格子数量；
  - Canvas 单帧绘制耗时。
- 只在开发环境或 URL 带 `?debug=perf` 时显示。

验收：

- 本地拖动、缩放、点击时能看到实时指标。
- 后续每个性能优化都能对比优化前后数字。

### 2. 给 WebSocket 消息加轻量统计日志

原因：
多人延迟很多时候不是“网络慢”，而是消息太大、太频繁、或处理太久。

要做：

- 客户端记录每条消息的字节数和处理耗时。
- 服务端记录每次 action 产生的更新格子数量。
- 对超过阈值的消息输出 warning，比如：
  - 单条消息超过 100KB；
  - 单次更新超过 500 个格子；
  - 单次 action 处理超过 50ms。

验收：

- 能知道卡顿是不是由大更新、大快照或频繁 cursor 导致。
- 不需要打开浏览器性能工具也能定位大方向。

## P1：棋盘渲染性能

### 3. 减少 Canvas 全量重绘成本

原因：
现在每次 `play.version` 变化、拖动、缩放都会画完整可视区域。可视区域越大、DPR 越高，成本越明显。

要做：

- 缓存 CSS 颜色，避免每帧多次 `getComputedStyle`。
- 只在主题变化或组件挂载时刷新颜色。
- 把静态格子背景绘制和动态内容绘制拆开评估。
- 对拖动过程考虑使用平移旧画面 + 补边，而不是每帧全量重画。
- 限制超高 DPR，比如移动端或大屏最多按 `devicePixelRatio = 2` 绘制。

验收：

- 拖动地图时 FPS 更稳定。
- Canvas 单帧绘制耗时明显下降。
- 高分屏设备不再因为 DPR 过高明显发热或掉帧。

### 4. 优化格子绘制函数

原因：
每帧会循环大量格子，`drawCell` 里的状态切换、字体设置、阴影、路径绘制都会被放大。

要做：

- 避免在每个格子里重复设置相同 font。
- 将旗子绘制预渲染到离屏 canvas，按颜色缓存。
- 将地雷图标改成矢量或离屏缓存，避免频繁绘制 emoji。
- 简化阴影和高光，优先保证清晰与流畅。
- 用整数坐标绘制，减少抗锯齿模糊和额外成本。

验收：

- 旗子很多时拖动仍然顺。
- 地雷/数字/旗子视觉不退步。
- 大屏可视格子数较多时绘制耗时下降。

### 5. 控制客户端格子缓存规模

原因：
`play.blocks` 会不断增长。玩家随机传送和长时间游玩后，本地缓存会越来越大。

要做：

- 记录每个格子的最近访问时间或是否在当前视野附近。
- 定期清理远离当前镜头、且不是已揭示/已插旗的临时隐藏格。
- 对从服务端来的真实格子保留，对客户端临时创建的空白格优先清理。
- 避免每个格子都用 Vue `reactive`，改成普通对象 + `version` 驱动 Canvas 重绘。

验收：

- 长时间游玩后内存不会持续上涨。
- 随机传送多次后 `play.blocks.size` 不会无限膨胀。
- Canvas 仍能正确显示服务端状态。

## P2：网络与同步性能

### 6. 初始快照按视野加载

原因：
现在新连接会收到所有已翻开/已插旗格子。世界越玩越大，首次进入越慢。

要做：

- 客户端连接后先发送当前视野中心和范围。
- 服务端只返回视野附近的 revealed/flagged 格子。
- 玩家移动到新区域时请求该区域快照。
- 保留排行榜和全局旗子数量，但棋盘格子改成区域化加载。

验收：

- 老世界也能快速进入。
- 首次 `init` 消息大小不会随着整个世界无限增长。
- 传送到远处时只加载目标附近区域。

### 7. WebSocket 更新消息瘦身

原因：
每个 block 更新现在会带完整字段。大量展开时，消息会迅速变大。

要做：

- 给格子更新定义紧凑格式。
- 只发送变化字段，而不是完整 block。
- 坐标可以用数组或短 key 表示，例如 `[x, y, flags, adjacent, ownerColor]`。
- 对大批量更新分片发送，避免单条消息过大。

验收：

- 同样一次大展开，消息体积明显变小。
- 客户端仍能正确合并状态。
- 大更新不会导致明显卡顿或 WebSocket 阻塞。

### 8. 降低 cursor 广播频率

原因：
cursor 每 50ms 发送一次，单人没问题，多人时会变成持续广播压力。

要做：

- 客户端从 50ms 调整到 100-150ms。
- 服务端再做一次节流，避免被改客户端刷爆。
- 鼠标不在棋盘内或窗口失焦时停止发送。
- 只在坐标变化超过一定距离时发送。

验收：

- 多人在线时光标仍然跟手。
- WebSocket 消息数量明显下降。
- 拖动地图时 cursor 不造成额外卡顿。

## P3：服务端与存储性能

### 9. 优化世界保存方式

原因：
`save()` 现在会从整个 `blocks` Map 里 filter 出 revealed/flagged，再整体写入 KV。世界越大，保存越重。

要做：

- 维护一个 `persistedBlocks` 或 dirty set，只跟踪需要持久化的格子。
- 保存时只处理 dirty blocks。
- 定期合并快照，而不是每次都扫描全量 Map。
- 给保存耗时和保存对象大小加日志。

验收：

- 世界变大后，单次保存耗时不会线性变慢。
- 高频点击不会造成 KV 写入堆积。
- KV 中的数据结构仍然能被旧版本迁移或读取。

### 10. 控制零展开规模

原因：
零展开可能一次性揭开大量格子，造成服务端 CPU、消息体积、客户端绘制同时变重。

要做：

- 给单次展开设置上限，比如最多 300-500 个格子。
- 如果超过上限，分批继续展开或提示“正在展开”。
- 大展开时合并计分更新，避免每格都触发排行榜重算。
- `updateLeaderboard()` 不要在每个格子加分后都执行。

验收：

- 大片空白区域不会造成明显卡顿。
- 排行榜更新仍然准确。
- 单次 action 的处理时间稳定在可接受范围内。

### 11. 排行榜更新降频

原因：
现在 `addScore()` 每次都更新排行榜。零展开时会触发很多次排序。

要做：

- 分数先累积，action 结束后统一更新排行榜。
- 或者给排行榜更新加 debounce。
- 给客户端发送最终排行榜，不必发送中间状态。

验收：

- 大量连续得分时 CPU 占用下降。
- 排行榜显示仍然实时到“操作级别”，不需要实时到“每个格子级别”。

## P4：加载与包体表现

### 12. 检查首屏资源大小

原因：
当前 UI、字体、Nuxt UI、Icon、音效逻辑都在首屏链路里。需要知道真实瓶颈在哪里。

要做：

- 用浏览器 Performance / Network 看首屏加载。
- 记录 JS、CSS、字体、图片大小。
- 检查 Nuxt UI 和 icon 是否引入过多。
- 对不影响首屏的面板或逻辑做延迟加载。

验收：

- 首屏资源清单清楚。
- 不必要的首屏资源被延迟。
- 线上冷启动打开更快。

### 13. 优化字体和图标加载

原因：
字体和图标资源会影响首屏观感，尤其是移动网络。

要做：

- 确认实际用了哪些字体权重。
- 减少不必要的字体文件。
- 图标只保留实际使用的集合。
- 关键 HUD 图标可考虑直接用已加载 icon 或轻量 SVG。

验收：

- 首屏字体请求更少。
- HUD 不会因为字体/图标加载造成明显闪动。

## 建议执行顺序

先做这 5 件，最容易看到效果：

1. 加性能调试面板。
2. 缓存 Canvas 颜色和旗子/地雷绘制。
3. 降低 cursor 发送和广播频率。
4. 零展开计分和排行榜合并更新。
5. 初始快照改成按视野加载。

这条路线更贴近你想要的“性能表现”：拖动更稳、打开更快、多人不抖、世界变大以后也不慌。
